import { FilePlus2, FolderOpen, SearchX, UserPlus } from "lucide-react";
import { Alert } from "../components/ui/alert";
import { Badge } from "../components/ui/badge";
import { Button } from "../components/ui/button";
import { Card, CardContent, CardHeader } from "../components/ui/card";
import { EmptyState } from "../components/ui/empty-state";
import { FormField } from "../components/ui/form-field";
import { Input } from "../components/ui/input";
import { Select } from "../components/ui/select";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "../components/ui/table";

export function DesignSystemPage() {
  return <div className="mx-auto max-w-7xl space-y-6"><section className="flex items-end justify-between"><div><p className="text-sm font-semibold text-primary">APPLICATION FOUNDATION</p><h2 className="mt-1 text-2xl font-bold tracking-tight">Administrative workspace</h2><p className="mt-1 text-sm text-muted-foreground">Reusable controls and layouts are ready for the upcoming barangay modules.</p></div><div className="flex gap-2"><Button variant="secondary"><FolderOpen className="size-4" />Open records</Button><Button><UserPlus className="size-4" />New resident</Button></div></section><Alert title="Development setup">Barangay and user details are placeholders until local settings and authentication are implemented.</Alert><section className="grid grid-cols-3 gap-5"><SpecimenCard title="Action buttons"><div className="flex flex-wrap gap-2"><Button>Primary action</Button><Button variant="secondary">Secondary</Button><Button variant="ghost">Tertiary</Button><Button variant="destructive">Remove</Button></div></SpecimenCard><SpecimenCard title="Status badges"><div className="flex flex-wrap gap-2"><Badge variant="success">Active</Badge><Badge variant="warning">Pending</Badge><Badge variant="destructive">Rejected</Badge><Badge variant="info">For review</Badge><Badge>Draft</Badge></div></SpecimenCard><SpecimenCard title="Field conventions"><div className="grid grid-cols-2 gap-3"><FormField label="Record type" htmlFor="record-type"><Select id="record-type" defaultValue=""><option value="" disabled>Select type</option><option>Resident</option></Select></FormField><FormField label="Reference no." htmlFor="reference"><Input id="reference" placeholder="e.g. RES-0001" /></FormField></div></SpecimenCard></section><section className="grid grid-cols-[1.6fr_1fr] gap-5"><Card><CardHeader><div><h3 className="text-sm font-bold">Future data table pattern</h3><p className="mt-0.5 text-xs text-muted-foreground">Search, filtering, sorting, pagination, status, and row actions can be composed consistently.</p></div><Button variant="secondary" size="sm">Filters</Button></CardHeader><Table><TableHeader><TableRow><TableHead>RECORD</TableHead><TableHead>TYPE</TableHead><TableHead>STATUS</TableHead><TableHead className="text-right">ACTIONS</TableHead></TableRow></TableHeader><TableBody><TableRow><TableCell className="font-semibold">No records yet</TableCell><TableCell>—</TableCell><TableCell><Badge>Not available</Badge></TableCell><TableCell className="text-right"><Button variant="ghost" size="sm">View</Button></TableCell></TableRow></TableBody></Table></Card><Card><CardHeader><h3 className="text-sm font-bold">Form layout pattern</h3></CardHeader><CardContent><div className="grid grid-cols-2 gap-4"><FormField label="First name" htmlFor="first-name"><Input id="first-name" placeholder="Enter first name" /></FormField><FormField label="Last name" htmlFor="last-name"><Input id="last-name" placeholder="Enter last name" /></FormField></div><p className="mt-4 text-xs text-muted-foreground">Related fields use two columns on desktop. Validation messages appear directly beneath the affected control.</p></CardContent></Card></section><EmptyState icon={SearchX} title="No dashboard data yet" description="Dashboard summaries, charts, requests, activities, and quick actions will be connected when their modules are implemented." action={<Button><FilePlus2 className="size-4" />Create first record</Button>} /></div>;
}

function SpecimenCard({ title, children }: { title: string; children: React.ReactNode }) { return <Card><CardHeader><h3 className="text-sm font-bold">{title}</h3></CardHeader><CardContent>{children}</CardContent></Card>; }
