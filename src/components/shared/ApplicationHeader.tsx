import { Bell, ChevronDown, KeyRound, LogOut, Search, UserRound } from "lucide-react";
import { Input } from "../ui/input";
import { Button } from "../ui/button";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "../ui/dropdown-menu";
import type { SessionUser } from "../../types/auth";

interface ApplicationHeaderProps { title: string; user: SessionUser; onLogout: () => void; onChangePassword: () => void; }
export function ApplicationHeader({ title, user, onLogout, onChangePassword }: ApplicationHeaderProps) { const initials=user.fullName.split(" ").map(x=>x[0]).slice(0,2).join(""); return <header className="flex h-16 shrink-0 items-center justify-between border-b border-border bg-white px-7"><div><h1 className="text-lg font-bold text-foreground">{title}</h1><p className="text-xs text-muted-foreground">Barangay Management System</p></div><div className="flex items-center gap-3"><div className="relative w-72"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><Input aria-label="Search records" placeholder="Search records..." className="pl-9" /></div><Button variant="ghost" size="icon" aria-label="Notifications"><Bell className="size-4" /></Button><div className="h-7 border-l border-border" /><DropdownMenu><DropdownMenuTrigger asChild><button type="button" className="flex items-center gap-2 rounded-md p-1 text-left hover:bg-muted"><span className="grid size-8 place-items-center rounded-full bg-[#dce7f5] text-xs font-bold text-primary">{initials}</span><span><span className="block text-sm font-semibold">{user.fullName}</span><span className="block text-xs text-muted-foreground">{user.roleName}</span></span><ChevronDown className="size-4 text-muted-foreground" /></button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem><UserRound className="mr-2 size-4" />Profile</DropdownMenuItem><DropdownMenuItem onSelect={onChangePassword}><KeyRound className="mr-2 size-4" />Change password</DropdownMenuItem><DropdownMenuItem onSelect={onLogout}><LogOut className="mr-2 size-4" />Logout</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div></header>; }
